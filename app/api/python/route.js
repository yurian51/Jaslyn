import { NextResponse } from "next/server";
import { z } from "zod";
import { runPythonTool } from "../../../src/bridge/python-runner.mjs";
const schema = z.object({ tool: z.enum(["system_snapshot", "workspace_list", "text_stats", "memory_store", "memory_search", "memory_list", "memory_delete", "api_key_create", "api_key_list", "api_key_revoke", "api_key_verify", "detect_language", "create_language", "run_jaslang"]), args: z.record(z.string(), z.unknown()).optional() });
export async function GET() { return NextResponse.json({ runtime: "python3", tools: [{ name: "system_snapshot", scope: "read-only" }, { name: "workspace_list", scope: "read-only" }, { name: "text_stats", scope: "pure" }, { name: "memory_store", scope: "persistent-write" }, { name: "memory_search", scope: "read-only" }, { name: "memory_list", scope: "read-only" }, { name: "memory_delete", scope: "persistent-delete" }, { name: "detect_language", scope: "read-only" }, { name: "create_language", scope: "workspace-write" }, { name: "run_jaslang", scope: "sandboxed-execution" }] }); }
export async function POST(request) { try { const parsed = schema.safeParse(await request.json()); if (!parsed.success) return NextResponse.json({ ok: false, error: "Unknown Python tool request." }, { status: 400 }); const result = await runPythonTool(parsed.data); return NextResponse.json({ ...result, runtime: "python3" }); } catch (error) { return NextResponse.json({ ok: false, runtime: "python3", error: error instanceof Error ? error.message : "Python worker failed." }, { status: 500 }); } }
