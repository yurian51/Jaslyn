"use client";
import Link from "next/link";
import { useState } from "react";
export default function LoginPage(){
 const [show,setShow]=useState(false),[email,setEmail]=useState(""),[password,setPassword]=useState(""),[error,setError]=useState(""),[loading,setLoading]=useState(false);
 async function submit(e){e.preventDefault();setError("");setLoading(true);try{const r=await fetch("/api/auth/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email,password})});const d=await r.json();if(!r.ok)throw new Error(d.error||"Sign in failed");window.location.assign("/net");}catch(err){setError(err.message)}finally{setLoading(false)}}
 return <main className="auth-shell"><div className="auth-visual"><div className="auth-grid"/><Link href="/" className="auth-brand"><span className="brand-symbol">⌁</span><span><strong>JASLYN <em>NET</em></strong><small>NETWORK OPERATING FABRIC</small></span></Link><div className="auth-visual-copy"><span className="eyebrow">PRIVATE OPERATIONS CONSOLE</span><h1>Welcome back<br/><span>to the network.</span></h1><p>Access your Jaslyn Net workspace and continue operating the infrastructure behind your subscribers.</p><div className="auth-signal"><i/> Secure workspace access <b>JASLYN / AUTH</b></div></div></div><section className="auth-panel"><div className="auth-card"><Link href="/" className="mobile-auth-brand">JASLYN <em>NET</em></Link><div className="auth-heading"><span className="eyebrow">SIGN IN</span><h2>Continue to Jaslyn Net</h2><p>Use your workspace credentials to open the operations console.</p></div><form onSubmit={submit}><label>Email address<input value={email} onChange={e=>setEmail(e.target.value)} type="email" placeholder="operator@company.com" autoComplete="email" required/></label><label>Password<div className="password-wrap"><input value={password} onChange={e=>setPassword(e.target.value)} type={show?"text":"password"} placeholder="Your password" autoComplete="current-password" required/><button type="button" onClick={()=>setShow(!show)}>{show?"Hide":"Show"}</button></div></label>{error&&<p role="alert" className="auth-error">{error}</p>}<div className="auth-row"><label className="check"><input type="checkbox"/><span>Remember this device</span></label><a href="#forgot">Forgot password?</a></div><button disabled={loading} className="auth-submit">{loading?"Signing in…":"Sign in"} <span>→</span></button></form><div className="auth-divider"><span>or</span></div><p className="auth-switch">New to Jaslyn Net? <Link href="/register">Create a workspace</Link></p><small className="auth-legal">Authentication is server-side; sessions use HttpOnly cookies.</small></div></section></main>
}