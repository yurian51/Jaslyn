{
  "name": "jaslyn",
  "version": "0.4.0",
  "private": true,
  "description": "Jaslyn, an independent autonomous AI agent runtime and control surface.",
  "type": "module",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "test": "node --test",
    "benchmark:health": "node scripts/benchmark-health.mjs",
    "benchmark:test": "node --test tests/benchmark/*.test.mjs",
    "benchmark:selftest": "node scripts/benchmark-selftest.mjs",
    "benchmark:security": "node --test tests/benchmark/security-regression.test.mjs"
  },
  "engines": { "node": ">=22" },
  "dependencies": {
    "next": "^15.5.0",
    "react": "^19.1.1",
    "react-dom": "^19.1.1",
    "zod": "^4.1.5"
  }
}
